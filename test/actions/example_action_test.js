// ////////////////////////////////////////////////
// THIS FILE IS FOR DEMONSTRATION PURPOSE ONLY //
// //////////////////////////////////////////////

/*
import { expect, mexpect } from '../test_helper';
import { loadCustomer } from '../../src/actions';
import { LOAD_CUSTOMER } from '../../src/actions/types';

const dummyCustomer = {
  name: 'Bas',
  surname: 'Yuksel',
};

describe('loadCustomer action', () => {
  let action;

  beforeEach(() => {
    action = loadCustomer(dummyCustomer);
  });

  it('should have the correct type', () => {
    mexpect(action.type).toEqual(LOAD_CUSTOMER);
  });

  it('should have the correct payload', () => {
    mexpect(action.payload).toEqual(dummyCustomer);
  });
});


*/
